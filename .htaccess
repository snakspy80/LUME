<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve the app through the public directory on hosts where the document root
    # cannot be changed, such as InfinityFree.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
